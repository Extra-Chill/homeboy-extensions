node_modules/
vendor/

# Transient runner artifacts (written through Playground VFS mount)
.pg-test-result.txt
.pg-bench-result.txt
.pg-bench-results.json

# Per-component homeboy.json generated by `homeboy component create` for
# bench/test fixtures registered locally — varies per machine, not part of
# the fixture contract.
tests/fixtures/*/homeboy.json
