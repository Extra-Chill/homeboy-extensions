# OpenCode AI Runtime

This package declares the experimental OpenCode agent-task runtime for Homeboy.
It is installed and discovered through the first-class `ai-runtimes/opencode`
runtime manifest rather than through a WordPress extension manifest.
