{
  "name": "Agent Hooks",
  "version": "1.0.0",
  "icon": "terminal.fill",
  "description": "Hooks for Claude Code and OpenCode to enforce Homeboy usage patterns",
  "author": "Extra Chill",
  "executable": {
    "runtime": {
      "setup_command": "bash {{extension_path}}/setup.sh",
      "run_command": "bash {{extension_path}}/run.sh {{args}}"
    }
  }
}
